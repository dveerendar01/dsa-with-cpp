#include<bits/stdc++.h>
using namespace std;
int main() {
    int i;
    for(int i=1; i<=5; i++) {
        cout << "Veerendar" << "\n";
    }          // i+=1 or i=i+1 or i++
    return 0;  // i-=1 or i=i-1 or i--
} // loop (or) for_loop is used to run the data how much we want--
  // -- or multiple times, according to the conditions.
    /* FOR_LOOP:
    for(initialization, condition, increment/decrement) {
            // code;
            }  */
// initialization: int i=1;
// condition: i<=5;
// increment: i+=1; // i=i+1; // i++
// decrement: i-=1; // i=i-1; // i--

