#include<iostream>
using namespace std; // if we use this std will run automatically, no need to write std::
int main() {
    cout << "Hey veerendar!" << endl << "Hey Veeru" << endl;
    cout << "Hey VEER";
    return 0;
}

/* endl is used to break the line.
    \n is also used to break the line.
    Difference between endl and \n is:
    endl is used to break the line and flush the buffer.
    \n is used to break the line but it doesn't flush the buffer. */