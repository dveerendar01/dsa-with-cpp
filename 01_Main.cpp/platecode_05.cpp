#include<iostream>
using namespace std;
int main() {
    int x,y;
    cin>>x>>y;
    cout<<"The value of x:"<<x<<" and y:"<<y;
    return 0;
}
// #include<bits/stdc++.h> it excicuts all the libraries, so it is more in use & recommanded insted of the #include<iostream>
// if we don't use (using namespace std;) then (std::) is compulsary.
// if we use (using namespace std;) then (std::) is no need to write.