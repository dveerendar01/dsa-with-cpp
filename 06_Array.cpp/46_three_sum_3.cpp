#include<bits/stdc++.h>
using namespace std;

vector<vector<int>> triplates(int n, vector<int>& nums) {
    vector<vector<int>> ans;
    sort(nums.begin(), nums.end());
    for(int i=0; i<n; i++) {

    }
}